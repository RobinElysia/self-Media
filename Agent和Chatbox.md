大家好，我是 ByteZen。

今天想聊一个最近特别容易被说乱的话题，就是 Agent。

很多人现在一提 Agent，脑子里就是各种很复杂的框架、MCP、Skills，或者几个特别火的 coding agent。但其实往前倒一步，你会发现 Agent 最开始的形态，就是我们每天都在用的 ChatBox。

所以这一期我不准备一上来就讲框架。

我们从最简单的聊天界面开始，一层一层往下拆：ChatBox 到底是什么，Agent 又多了什么，然后模型 API、框架、MCP、Skills 分别在里面扮演什么角色。

最后再聊一个真正实用的问题：这些东西到底应该怎么用，才能让 Agent 长时间稳定地干活。

# 什么是 ChatBox？

先从 ChatBox 开始。

我们平时看到的，就是一个聊天窗口。

你输入一句话，它把消息发给模型，模型再返回一个答案。看起来特别简单，但实际上，真正送给模型的东西，远不止你刚才输入的那句话。

还有系统提示词、之前聊过什么、上传过哪些文件，甚至有时候还有检索出来的内容。

所以如果一定要给 ChatBox 下一个比较准确的定义，我更倾向于把它看成一个“上下文管理器”。

它的核心目标还是让模型回答问题。

也就是说，模型主要负责生成答案，而 ChatBox 主要负责把“你现在到底在聊什么”这件事情整理好。

# Context 和 Prompt

这时候就会碰到两个特别重要的概念：Prompt 和 Context。

这两个东西经常被混在一起，但其实分工完全不同。

Prompt 更像是在告诉模型，你应该怎么工作。

比如你的角色是什么，目标是什么，有哪些约束，最后输出要长什么样。

而 Context 是告诉模型，你现在手上到底有哪些信息。

比如前面的聊天记录、项目文件、工具返回结果，还有当前任务做到哪一步了。

再往前一步，还有 Memory。

Memory 解决的是另外一个问题：哪些东西值得跨很多轮对话一直留下来，而不是每次都重新塞进去。

所以我一般会这么记：Prompt 决定“怎么做”，Context 决定“依据什么做”。

而这其实已经非常接近 Agent 工程的核心了。

# 什么是 Agent？

那 Agent 到底是什么？

我的理解其实很简单。

Agent 本质上不是“更聪明的聊天机器人”，而是一套能够围绕目标不断采取行动的应用系统。

它先观察当前状态，然后决定下一步做什么。

决定以后，就去调用工具执行。

执行完了，再回来看看结果对不对。

如果不对，就继续调整。

所以你会发现，它和 ChatBox 最大的区别，并不是聊天界面变了，而是中间多了一整个行动闭环。

也就是我们这里看到的四步：Observe，Decide，Act，再 Verify。

所以最后可以浓缩成一句话：Agent 等于 Model，加上 Context、Tools，再加上一个 Loop。

# 什么是大模型厂商 API？

那这个 Agent 到底靠什么驱动？

这里就轮到模型 API 了。

其实所谓“大模型厂商 API”，你可以把它理解成模型能力的程序化入口。

你的 ChatBox 或者 Agent，先把请求发过去。

这里面会带上身份认证、输入内容，甚至还有工具定义。

然后模型负责推理。

它可以直接返回文本，也可以返回 JSON，或者告诉你的程序：“下一步我要调用这个工具。”

然后你的程序再把这个结果接住。

所以 API 本身不是 Agent。

它只是模型能力的入口。

真正让 Agent 动起来的，是 API 外面那层程序，把模型一次次的输出重新接回来，继续执行。

# 代表性模型厂商

现在比较主流的几家，实际上都在做类似的事情，只是接口和生态各有区别。

比如 OpenAI，它现在的 API 已经不只是单纯的文本生成，而是把工具调用、文件搜索、网页搜索、MCP 这些能力一起放进了更完整的调用体系里。

Anthropic 这边，大家比较熟悉的是 Claude，它的 Messages API 也原生支持 Tools。

Google 的 Gemini 则比较强调多模态、Function Calling、长上下文，以及面向 Agent 工作流的一些接口。

DeepSeek 的特点大家也比较熟悉，它同样提供 Tool Calls，而且可以配合结构化输出和多轮工具调用。

再比如 Qwen，阿里云百炼除了自己的模型 API，也提供了 OpenAI-compatible 的调用方式，对开发者来说迁移成本会低很多。

所以你会发现，大家最后都在解决同一个问题：

怎么让模型不只是“回答”，而是能够和外部程序一起工作。

# API 很像，但接口哲学不同

不过这里特别容易产生一个误解。

你会觉得既然大家都有 API，那是不是换个模型地址就行了？

实际上没那么简单。

最底层的 Model Call 很简单，就是我把输入给你，你还给我结果。

再往上一层，是 Tool Call。

这个时候模型不一定直接回答，而是告诉你的程序：“帮我调用这个函数。”

然后程序执行完，再把工具结果送回模型。

再往上，就是 Agent Runtime。

它开始负责管理多轮调用、上下文、状态、工具、停止条件。

所以我们看到的那些 Agent 产品，实际上已经不是简单的“调用一次模型 API”了，而是在 API 上面再包了一层运行时。

# 自主探索和操作到底怎么来的？

所以现在问题就来了。

很多人说 Agent 可以“自主探索”，甚至“自主操作”。

这个自主到底是从哪里来的？

其实没有那么神秘。

不是模型突然有了意识，也不是它突然产生了真正意义上的自主性。

本质上，是程序给了它一个连续行动的闭环。

我给你一个目标。

然后把当前环境状态给你。

你可以调用这些工具。

你做完以后，我把结果重新告诉你。

然后你继续判断下一步。

所以 Agent 的“自主”，更接近一种工程意义上的连续决策。

模型负责决定下一步怎么走，系统负责真的让这一步发生。

# Agent Framework 是什么？

那既然自己写也能跑，为什么现在还会出现这么多 Agent Framework？

原因非常现实。

因为如果不用框架，这些东西你都得自己写。

模型什么时候调用？

工具怎么 dispatch？

失败了怎么办？

要不要重试？

上下文怎么维护？

什么时候停？

状态怎么保存？

这些东西一多，真正麻烦的其实已经不是模型了，而是外围工程。

所以框架真正的价值，不是把模型变聪明。

而是把 Agent runtime 里面那些重复，而且很容易出错的东西封装起来。

你自己造 Loop，控制力当然最高。

但如果借助框架，你可以直接复用很多已经设计好的 Agent 能力。

# 常见 Agent Framework

所以现在比较常见的框架，大概可以这么理解。

LangChain 更偏通用，它把模型、工具、Agent 这些东西组合起来，让你比较快地搭一个应用。

LangGraph 则更强调状态和图式编排。

尤其当任务开始出现分支、循环、持久化这些需求的时候，它会明显更舒服。

OpenAI Agents SDK 更接近“用比较少的核心原语，把 Agent 跑起来”。

然后是 AutoGen，它更强调多 Agent 和 runtime。

CrewAI 则比较有意思，它把 Agent 的角色感做得更明显，比如不同 Agent 扮演不同职责，再通过 Flow 把工作串起来。

所以这些东西虽然都叫 Agent Framework，但它们解决的问题其实不完全一样。

不要因为它们名字都叫 Agent，就默认它们是一回事。

# 怎么选框架？

那框架这么多，到底应该怎么选？

我反而觉得，不要先问“哪个框架最好”。

先问一个问题：你到底想把控制权放在哪里？

如果你只是想很快做出一个 Agent，那就可以选择抽象层更高的东西，让框架帮你处理大部分 runtime。

但如果你的任务特别复杂，你需要自己定义状态、路由、checkpoint，甚至 human-in-the-loop，那你就会更需要可控性。

另外，不同领域也不一样。

做数据驱动应用的人，可能更偏向 LlamaIndex。

需要复杂多 Agent 协作的，可能会看 AutoGen、CrewAI。

如果你本身就在 OpenAI 的技术体系里，Agents SDK 也很自然。

所以不存在一个框架可以覆盖所有场景。

真正重要的是，你先知道自己的 Agent 到底复杂在哪里。

# 有哪些 Agent？

接下来这个问题就更有意思了。

既然 Agent 已经是一种完整的应用系统，那现在市场上到底有哪些？

最典型的一类，就是 coding agent。

比如 OpenAI 的 Codex。

它的核心逻辑其实非常直观：理解仓库，修改代码，运行命令，然后验证结果。

Anthropic 这边是 Claude Code，它直接进入你的项目环境，读文件、改文件、执行命令。

Google 有 Gemini CLI，也是非常典型的 terminal-first Agent。

再往产品层看，还有 Cursor Agent，它直接把 Agent 放进了编辑器里。

Devin 则更偏向一个完整的软件工程 Agent，目标不只是改一行代码，而是承担更长周期的工程任务。

OpenHands 则是比较有代表性的开源方向。

所以现在的 Agent，其实已经不只是一种产品，而是正在形成一整个产品谱系。

# 为什么这些 Agent 看起来这么像？

但你把这些产品放在一起，会发现它们长得其实特别像。

为什么？

因为它们底层都在解决差不多的问题。

第一层是 Model。

模型负责理解、推理，然后决定下一步。

第二层是 Harness。

也就是包在模型外面的那套运行机制，负责规则、上下文、权限和状态。

第三层是 Tools。

比如文件、Shell、搜索、网络。

最后是 Workspace，也就是 Agent 真正工作的地方。

所以你以后再去看一个新的 Agent 产品，可以别只看它底层用的是什么模型。

你更应该看它外面的 Harness 到底做得怎么样。

因为真正决定“它能不能干活”的，往往就在这里。

# MCP：把外部能力接进 Agent

那如果 Agent 原本只能操作文件和终端，但我还想让它访问 GitHub、数据库、内部 SaaS，怎么办？

这就轮到 MCP 了。

MCP 可以理解成一套比较标准化的连接方式。

Agent 不需要为每个外部系统都重新写一套适配逻辑。

它通过 MCP Server 接出去，而 MCP Server 再把工具或者资源暴露出来。

所以这里最重要的一点是：

MCP 扩展的是 Agent 的能力边界。

它让 Agent 原本碰不到的数据和系统，开始变得可访问、可操作。

但反过来，接得越深，你就越需要考虑权限、认证和安全边界。

所以 MCP 很强，但它也意味着更强的系统耦合。

# Skills：把知识和流程变成资产

那 Skills 又是什么？

我觉得这个东西非常容易被理解错。

很多人会把 Skill 理解成一个“更厉害的插件”。

但其实它更像一份给 Agent 用的操作手册，只不过这份手册不仅可以写文字，还可以配脚本。

比如你可以把公司的代码规范、某个项目的开发流程、部署方式、测试要求，全部沉淀下来。

这些内容可以是 Markdown。

也可以有一些实际执行的脚本。

这样以后每次 Agent 进入这个项目，就不需要从零开始解释。

所以 Skill 更适合解决“怎么做”这个问题。

从这个角度看，它特别适合做知识蒸馏。

不是重新训练一个模型，而是把已经验证过的方法压缩成 Agent 可以反复复用的知识和流程。

# MCP 和 Skills 到底有什么区别？

所以到这里，这两个东西其实已经很好区分了。

你可以粗暴一点把它们理解成：

MCP 更偏“手”。

Skills 更偏“操作手册”。

MCP 让 Agent 获得新的外部能力。

比如访问 GitHub、数据库、搜索服务。

Skill 则告诉 Agent，一件事情应该按照什么方式去完成。

所以一个非常简单的组合就是：

Skill 告诉它规则。

MCP 给它工具。

两者甚至可以一起用。

比如一个代码审查 Skill，里面写清楚代码审查标准。

然后通过 MCP 去访问 GitHub 的 Pull Request。

这样 Agent 才真正拥有“按这个方法去做这件事情”的能力。

# 如何高效使用 Agent？看 Codex CLI

到了这里，我觉得最重要的问题终于来了。

这些东西我们都知道了，但真正用 Agent 的时候，怎么才能效率高一点？

这里我比较喜欢拿 Codex CLI 举例。

很多人第一次用 Agent，会不断地给它补 Prompt。

走两步，发现上下文乱了，再补一句。

再走两步，又继续补。

最后整个会话变得特别长。

其实更有效的方法，是把 Agent 当成一个长期工作的同事。

比如你进入一个项目之后，不一定马上让它改东西。

可以先让它理解仓库结构，找到对应模块，再开始执行。

任务做到一个阶段之后，还可以用 `/compact` 主动整理当前上下文。

所以真正重要的，不是你写了多少 Prompt。

而是你有没有在管理 Agent 的工作状态。

# /compact 到底在干什么？

那 `/compact` 到底有什么意义？

你可以把一个很长的 Agent 会话想象成一张越来越乱的桌子。

上面堆着聊天记录、命令、文件、失败尝试，还有各种已经不重要的信息。

如果一直往上堆，当然会越来越重。

所以 `/compact` 的核心思路，就是把这些历史过程重新整理一下。

哪些东西已经不重要了，可以压缩。

哪些东西还影响下一步，要留下。

最后形成一个更紧凑的工作状态，再继续往下做。

所以我更愿意把它理解成“重建上下文”，而不只是简单地删除聊天记录。

尤其是长任务里，这件事情非常重要。

因为 Agent 越往后做，真正有价值的往往已经不是最开始那些过程，而是现在的状态、约束和没完成的事情。

# 让 Agent 更像项目成员

再往上走一步，有两个特别实用的技巧。

第一个，是项目入口。

比如 `AGENTS.md`。

你可以把项目的技术栈、目录边界、代码规范、验证方式，还有一些明确禁止做的事情写进去。

这样 Agent 每次进入项目，都会有一个稳定的起点。

第二个，是 Sub-Agent。

不要让一个 Agent 一个人处理所有事情。

比如一个主 Agent 负责拆任务。

Research Agent 专门查资料。

Coding Agent 专门写代码。

Review Agent 最后负责检查。

这样每个 Agent 的上下文都更窄，职责也更明确。

其实你会发现，这已经越来越像一个真正的工程团队了。

而 Agent 工程做到最后，本质上也是在做这件事情：把一个大问题，拆成一组可以管理的小问题。

# 从 ChatBox 到 Agent

所以把整套东西重新串起来，其实并没有那么复杂。

最开始，我们只是让模型回答问题。

然后开始管理 Context，让模型知道现在发生了什么。

接着给它 Tools，让它能够真正操作东西。

再通过 Loop，让这些操作可以连续发生。

最后再用 Harness、权限、流程、Skills、MCP 和验证机制，把整个过程工程化。

所以我现在越来越觉得，Agent 真正难的地方，其实不是“怎么把模型接进来”。

模型 API 现在已经越来越容易用了。

真正难的是：

你给它什么目标？

让它知道什么？

允许它做什么？

做错了怎么办？

什么时候应该停？

以及，做完以后你怎么确认它真的做对了？

Model 是引擎。

Agent 是整车。

真正决定这辆车能不能跑起来的，其实是它外面的整个系统。

我是 ByteZen，我们下一期再继续往 Agent 工程里面拆。